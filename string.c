#include <stdio.h>
#include <string.h>

int main()
{

    char name[20], email[50];

    // int roll;

    printf("enter your name: \n");
    gets(name);
    puts(name);

    // printf("enter your roll number: \n");
    // gets(roll);
    // puts(roll);

    printf("enter your email: \n");
    gets(email);
    puts(email);

    return 0;
}